/*void foreward(int stepNumber)
{


  for (int x = 0; x < 5/*(stepNumber*16)*//*; x++)
  {
    digitalWrite(STEPPIN, HIGH);
    delayMicroseconds(stepDelay);
    digitalWrite(STEPPIN, LOW);
    delayMicroseconds(stepDelay);

  }
}
void backward(int stepNumber)
{
  // Set motor direction counterclockwise
  invert(DIRPIN);

  for (int x = 0; x < 5/*( stepNumber *16)*//*; x++)
  {
    digitalWrite(STEPPIN, HIGH);
    delayMicroseconds(stepDelay);
    digitalWrite(STEPPIN, LOW);
    delayMicroseconds(stepDelay);

  }
  invert(DIRPIN);
}*/
